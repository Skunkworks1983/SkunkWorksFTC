This is the psudocode for the logic of how the program will work.

if (rotation > 50 && rotation < 40 && distance > 10) {
    drive the bot forward
} else {
    if (rotation < 50){
        turn robot left
    }
    if (rotation > 40) {
        turn robot right
    }
    if (distance < 10) {
        track line on ground to beacon
    }
}